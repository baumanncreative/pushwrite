# PushWrite 0.3.0 Security Review

Date: 2026-08-10  
Baseline Codex Security scan: `994d1f07-8c9d-4abd-a6b4-f0cf07ee789a`
Baseline result: passed with 0 reportable findings

## Scope

The review covered the complete PushWrite 0.3.0 working tree, including:

- microphone capture and the in-memory WAV handoff
- bundled Whisper and local text-model execution
- offline sandboxing and subprocess boundaries
- Accessibility focus binding and all text-insertion routes
- protected-content handling and production log redaction
- runtime-directory creation, file validation and cleanup
- bundled runtime and model integrity verification
- QA-only control interfaces and validation fixtures
- build, signing, notarization, packaging and installation scripts
- CI permissions, dependency provenance and secret checks

Generated build caches, opaque model internals, unrelated vendored targets and external account state were excluded. Their reachable boundaries, pinned revisions, sizes, hashes and build configurations were reviewed.

## Validated security properties

- Production audio, transcripts and local-model prompts remain local and are passed through memory-backed pipes.
- Whisper and local text processing run in fail-closed child sandboxes with network and file-write restrictions.
- Production accepts only bundled runtimes and models whose exact paths, file types, sizes and SHA-256 digests are verified immediately before use.
- Accessibility insertion remains bound to the receipt-time process and focused element and is revalidated before each write.
- Protected or unknown fields fail closed.
- Control characters are rejected. Multiline insertion is allowed only for an explicit document-editor allowlist, so unknown applications and embedded IDE consoles fail closed.
- Production JSON logging recursively redacts text, values and titles; production transcription artifacts are disabled.
- Runtime paths reject symlinks and cross-owner objects, use private directories and restrict cleanup to recognized internal artifact names.
- The authenticated Apple release path requires a Developer ID Application identity, an exact Apple Team ID and a notary profile before emitting artifacts. ZIP and DMG outputs on that path must pass signing, notarization, stapling and Gatekeeper checks.
- The explicitly unsigned GitHub direct-download path requires an opt-in acknowledgement, fresh native runtime builds, exact ad-hoc signature/architecture/minimum-OS checks, ZIP install validation, mounted-DMG payload validation, per-asset size enforcement, GitHub Actions/Sigstore build-provenance attestations and an immutable GitHub release. Raw local product output remains non-publishable.

## Verification evidence

- Codex Security standard baseline scan: 0 findings, complete coverage, sealed 2026-08-10
- Final release-diff gate: a sealed scan of the exact release commit must pass before merge; its scan ID and result are recorded in the GitHub pull-request release evidence
- unit tests: 10 passed
- product UI tests: 2 passed
- security regression suite: passed
- local text transformations: 12 language directions passed
- production and QA application builds: passed
- strict deep code-signature structure verification: passed for both local ad-hoc test bundles
- bundled English audio to Whisper to local German translation to TextEdit insertion: passed, using `accessibilitySelectedText`
- stable release credential gate: rejected missing credentials before writing output

## External release prerequisites and distribution decision

The local machine currently has no Developer ID Application identity and no configured notarytool profile. Consequently, the authenticated Apple release builder intentionally fails before producing output.

For version 0.3.0, the product owner selected direct distribution outside the Mac App Store without Apple notarization. Public assets must therefore be produced only by the dedicated GitHub Actions release workflow, cryptographically attested by GitHub/Sigstore, clearly suffixed `-unsigned`, and locked in an immutable GitHub release. The release must not be described as Apple-verified, and installation instructions must retain the expected Gatekeeper override.

The pinned Swiss German five-sample validation passed previously with aggregate WER `0.378` against the `0.400` threshold. A fresh retrieval could not be completed during the final local run because local processes could not establish HTTPS connections to the Hugging Face dataset endpoints; the repository revision and first fixture rows were independently confirmed through the web interface.

## Residual operational risks

- The direct-download build is not recognized by macOS as an identified Apple developer; users may need to approve the first launch manually in Privacy & Security.
- GitHub/Sigstore provenance authenticates the repository workflow and build origin but does not replace Gatekeeper or Apple's malware notarization service.
- A future Apple-authenticated artifact still requires publisher credentials and independent signing/notarization validation.
- The pinned Swiss German corpus test should be rerun when the local execution environment can reach the dataset endpoints.
- The QA build exposes test controls by design and must never be distributed as the production application.
